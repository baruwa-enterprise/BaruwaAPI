machine:
  python:
    version: '2.6.6'
dependencies:
  pre:
      - pip install -r requirements/requirements26_dev.txt
test:
    override:
        - python setup.py nosetests
    post:
        - pip install codecov && codecov
